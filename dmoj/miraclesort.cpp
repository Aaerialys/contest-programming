#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n,x;
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>x;
        cout<<x<<"\n";
    }
    return 0;
}
#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);
    cout<<"Can sample output exist without sample input? If a tree falls in a forest and no one is around to hear it, does it make a sound? Is the previous question even relevant?";
    return 0;
}